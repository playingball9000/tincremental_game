{
  "name": "incremental-game",
  "version": "1.0.0",
  "description": "An incremental game built with TypeScript",
  "main": "main.js",
  "scripts": {
    "build": "tsc --outDir dist && cp index.html dist",
    "start": "tsc -w",
    "clean": "rm -rf dist",
    "dev": "npm run clean && npm run build && cd dist && http-server -o"
  },
  "dependencies": {
    "@types/node": "^20.11.19"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  }
}
