{
  "name": "incremental-game",
  "version": "1.0.0",
  "description": "An incremental game built with TypeScript",
  "main": "main.js",
  "scripts": {
    "build": "npx webpack && cp index.html dist && cp styles.css dist && cd dist",
    "start": "tsc -w",
    "clean": "rm -rf dist",
    "dev": "npm run clean && npm run build && http-server -c-1 -o"
  },
  "dependencies": {
    "@types/node": "^20.11.19"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.3",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4"
  }
}
